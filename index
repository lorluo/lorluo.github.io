<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Your Name</title>
  <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@300;400;600;700&display=swap" rel="stylesheet">
  <style>
    @import url('https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@300;400;600;700&display=swap');

    * { margin: 0; padding: 0; box-sizing: border-box; }

    :root {
      --bg: #2A1F1D;
      --text: #DEC165;
      --accent: #BE8A13;
      --muted: #7F6A55;
      --link: #7CC9CF;
      --link-hover: #FFCB1B;
      --headline: #FFEAA3;
    }

    html, body {
      background-color: var(--bg);
      color: var(--text);
      font-family: 'JetBrains Mono', monospace;
      font-size: 15px;
      line-height: 1.6;
      min-height: 100vh;
    }

    body { padding: 40px 20px; }

    .container {
      max-width: 720px;
      margin: 0 auto;
    }

    .avatar-container { text-align: center; margin-bottom: 60px; }
    .avatar {
      width: 360px;
      height: 360px;
      object-fit: cover;
      border: 2px solid var(--muted);
      padding: 2px;
      background: var(--bg);
    }

    h1 {
      font-size: 32px;
      font-weight: 700;
      text-align: center;
      margin-bottom: 60px;
      color: var(--headline);
      letter-spacing: -0.02em;
    }

    .section { margin-bottom: 48px; }
    .section-title {
      font-size: 13px;
      font-weight: 600;
      color: var(--accent);
      margin-bottom: 16px;
      text-transform: uppercase;
      letter-spacing: 0.1em;
    }

    .link-item { margin-bottom: 12px; padding-left: 16px; position: relative; }
    .link-item::before {
      content: "→";
      position: absolute;
      left: 0;
      color: var(--muted);
      transition: color 0.2s ease;
    }
    .link-item:hover::before { color: var(--link-hover); }
    .link-item a {
      color: var(--text);
      text-decoration: none;
      transition: color 0.2s ease;
      font-size: 16px;
    }
    .link-item a:hover { color: var(--headline); }

    .contact-info { font-size: 14px; line-height: 1.8; color: #BFC65A; }
    .contact-info a { color: var(--link); text-decoration: none; transition: color 0.2s ease; }
    .contact-info a:hover { color: var(--text); }

    ::selection { background: var(--accent); color: var(--bg); }

    @media (max-width: 768px) {
      body { padding: 30px 20px; font-size: 15px; }
      h1 { font-size: 28px; margin-bottom: 40px; }
      .avatar { width: 150px; height: 150px; }
      .section { margin-bottom: 36px; }
    }
  </style>
</head>
<body>
  <div class="container">
    
    <!-- Avatar -->
    <div class="avatar-container">
      <img class="avatar" src="https://via.placeholder.com/360" alt="Your Avatar">
    </div>

    <!-- Heading -->
    <h1>Your Name</h1>

    <!-- About Section -->
    <div class="section">
      <div class="section-title">About Me</div>
      <p>Hello! I'm a developer, designer, or something else entirely. I love building clean and creative websites. This is a simple site hosted on GitHub Pages.</p>
    </div>

    <!-- Links -->
    <div class="section">
      <div class="section-title">Links</div>
      <div class="link-item"><a href="https://github.com/yourusername" target="_blank">GitHub</a></div>
      <div class="link-item"><a href="https://linkedin.com/in/yourprofile" target="_blank">LinkedIn</a></div>
      <div class="link-item"><a href="https://yourblog.com" target="_blank">Blog</a></div>
    </div>

    <!-- Contact -->
    <div class="section">
      <div class="section-title">Contact</div>
      <div class="contact-info">
        Email: <a href="mailto:you@example.com">you@example.com</a><br />
        Twitter: <a href="https://twitter.com/yourhandle">@yourhandle</a>
      </div>
    </div>

  </div>
</body>
</html>
